import cart from "./assets/react.svg"

const CartWidget = () => {
    return (
        <div>
            <img src={cart} alt= "cart-wdiget"/>
            0
        </div>
    )
}

export default CartWidget